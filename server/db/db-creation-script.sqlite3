PRAGMA FOREIGN_KEYS = 0;
DROP TABLE IF EXISTS `USER`;
DROP TABLE IF EXISTS `BOOK`;
DROP TABLE IF EXISTS `SONG`;
DROP TABLE IF EXISTS `SONG_VARIANT`;
DROP TABLE IF EXISTS `SONG_IMAGE_DATA`;
DROP TABLE IF EXISTS `SONG_IMAGES`;
DROP TABLE IF EXISTS `VARIANT_LAST_OPENED_DATE`;
DROP TABLE IF EXISTS `PLAYLIST`;
DROP TABLE IF EXISTS `SONG_IN_PLAYLIST`;
DROP TABLE IF EXISTS `USER_SETTINGS`;
DROP TABLE IF EXISTS `USER_SONG_SEARCH`;
DROP TABLE IF EXISTS `USER_FAVORITE_BOOKS`;
DROP TABLE IF EXISTS `PLAYLIST_TEMPLATE`;
DROP TABLE IF EXISTS `PLAYLIST_TEMPLATE_ITEM`;
DROP TABLE IF EXISTS `USER_HAS_IN_LIBRARY`;
DROP TABLE IF EXISTS `BOOK_CUSTOM_PERMISSION`;
PRAGMA FOREIGN_KEYS = 1;

CREATE TABLE `USER` (
	`Id` INTEGER AUTO_INCREMENT  NOT NULL,
	`Username` VARCHAR(30) NOT NULL,
	`Salt` INTEGER NOT NULL,
	`Hash` CHAR(256) NOT NULL,
	`Email` TEXT NOT NULL,
	`Name` TEXT NOT NULL,
	`Surname` TEXT NOT NULL,
	`Language` TEXT NOT NULL,
	`OfflineData` BOOLEAN NOT NULL,
	`Column1` INTEGER NOT NULL,
	PRIMARY KEY (`Id`)
);

CREATE TABLE `BOOK` (
	`Id` INTEGER NOT NULL,
	`Owner` INTEGER NOT NULL,
	`Title` TEXT NOT NULL,
	`Description` TEXT NOT NULL,
	`GeneralPermission` CHAR,
	`Cover` TEXT,
	`UnconfirmedNewOwner` INTEGER,
	PRIMARY KEY (`Id`),
	FOREIGN KEY (`Owner`) REFERENCES `USER`(`Id`),
	FOREIGN KEY (`UnconfirmedNewOwner`) REFERENCES `USER`(`Id`)
);

CREATE TABLE `SONG` (
	`Id` INTEGER AUTO_INCREMENT NOT NULL,
	`BookId` INTEGER NOT NULL,
	`Title` TEXT NOT NULL,
	PRIMARY KEY (`Id`),
	FOREIGN KEY (`BookId`) REFERENCES `BOOK`(`Id`)
);

CREATE TABLE `SONG_VARIANT` (
	`Id` INTEGER AUTO_INCREMENT NOT NULL,
	`SongId` INTEGER NOT NULL,
	`Name` TEXT NOT NULL,
	`Notes` TEXT NOT NULL,
	FOREIGN KEY (`SongId`) REFERENCES `SONG`(`Id`)
);

CREATE TABLE `SONG_IMAGE_DATA` (
	`Id` INTEGER AUTO_INCREMENT NOT NULL,
	`SongId` INTEGER NOT NULL,
	`VariantId` INTEGER NOT NULL,
	`Data` TEXT NOT NULL,
	PRIMARY KEY (`Id`)
);

CREATE TABLE `SONG_IMAGES` (
	`Id` INTEGER AUTO_INCREMENT NOT NULL,
	`VariantId` INTEGER NOT NULL,
	`PageNumber` INTEGER NOT NULL,
	`ImageId` INTEGER NOT NULL,
	`Annotations` TEXT,
	PRIMARY KEY (`VariantId`, `PageNumber`),
	FOREIGN KEY (`VariantId`) REFERENCES `SONG_VARIANT`(`Id`),
	FOREIGN KEY (`ImageId`) REFERENCES `SONG_IMAGE_DATA`(`Id`)
);

CREATE TABLE `VARIANT_LAST_OPENED_DATE` (
	`UserId` INTEGER NOT NULL,
	`VariantId` INTEGER NOT NULL,
	`Date` DATE NOT NULL,
	PRIMARY KEY (`UserId`, `VariantId`),
	FOREIGN KEY (`UserId`) REFERENCES `USER`(`Id`),
	FOREIGN KEY (`VariantId`) REFERENCES `SONG_VARIANT`(`Id`)
);

CREATE TABLE `PLAYLIST` (
	`Id` INTEGER AUTO_INCREMENT NOT NULL,
	`Owner` INTEGER NOT NULL,
	`Date` DATE NOT NULL,
	`Name` TEXT NOT NULL,
	PRIMARY KEY (`Id`),
	FOREIGN KEY (`Owner`) REFERENCES `USER`(`Id`)
);

CREATE TABLE `SONG_IN_PLAYLIST` (
	`PlaylistID` INTEGER NOT NULL,
	`Label` VARCHAR(20) NOT NULL,
	`SongId` INTEGER,
	`Index` INTEGER NOT NULL,
	`Color` VARCHAR(7) NOT NULL,
	PRIMARY KEY (`PlaylistID`, `Label`),
	FOREIGN KEY (`PlaylistID`) REFERENCES `PLAYLIST`(`Id`),
	FOREIGN KEY (`SongId`) REFERENCES `SONG`(`Id`)
);

CREATE TABLE `USER_SETTINGS` (
	`UserId` INTEGER AUTO_INCREMENT NOT NULL,
	`Language` TEXT NOT NULL,
	`StoreDataOffline` BOOLEAN NOT NULL,
	`PcShortcutNextPage` VARCHAR(10) NOT NULL,
	`PcShortcutPrevPage` VARCHAR(10) NOT NULL,
	`Theme` CHAR NOT NULL,
	`MultiplePagesScroll` BOOLEAN NOT NULL,
	`ScrollGesture` CHAR NOT NULL,
	`TouchGesture` CHAR NOT NULL,
	PRIMARY KEY (`UserId`),
	FOREIGN KEY (`UserId`) REFERENCES `USER`(`Id`)
);

CREATE TABLE `USER_SONG_SEARCH` (
	`UserId` INTEGER NOT NULL,
	`SongId` INTEGER NOT NULL,
	`Date` DATE NOT NULL,
	PRIMARY KEY (`UserId`, `SongId`, `Date`),
	FOREIGN KEY (`UserId`) REFERENCES `USER`(`Id`),
	FOREIGN KEY (`SongId`) REFERENCES `SONG`(`Id`)
);

CREATE TABLE `USER_FAVORITE_BOOKS` (
	`UserId` INTEGER NOT NULL,
	`BookId` INTEGER NOT NULL,
	PRIMARY KEY (`UserId`, `BookId`),
	FOREIGN KEY (`UserId`) REFERENCES `USER`(`Id`),
	FOREIGN KEY (`BookId`) REFERENCES `BOOK`(`Id`)
);

CREATE TABLE `PLAYLIST_TEMPLATE` (
	`Id` INTEGER AUTO_INCREMENT NOT NULL,
	`Name` VARCHAR(30) NOT NULL,
	`Owner` INTEGER NOT NULL,
	PRIMARY KEY (`Id`),
	FOREIGN KEY (`Owner`) REFERENCES `USER`(`Id`)
);

CREATE TABLE `PLAYLIST_TEMPLATE_ITEM` (
	`Id` INTEGER AUTO_INCREMENT NOT NULL,
	`Label` VARCHAR(20) NOT NULL,
	`TemplateId` INTEGER NOT NULL,
	`Index` INTEGER NOT NULL,
	`Color` VARCHAR(7) NOT NULL,
	PRIMARY KEY (`Id`),
	FOREIGN KEY (`TemplateId`) REFERENCES `PLAYLIST_TEMPLATE`(`Id`)
);

CREATE TABLE `USER_HAS_IN_LIBRARY` (
	`UserID` INTEGER NOT NULL,
	`BookID` INTEGER NOT NULL,
	PRIMARY KEY (`UserID`, `BookID`),
	FOREIGN KEY (`UserID`) REFERENCES `USER`(`Id`),
	FOREIGN KEY (`BookID`) REFERENCES `BOOK`(`Id`)
);

CREATE TABLE `BOOK_CUSTOM_PERMISSION` (
	`UserID` INTEGER NOT NULL,
	`BookID` INTEGER NOT NULL,
	`Permission` CHAR NOT NULL,
	PRIMARY KEY (`UserID`, `BookID`),
	FOREIGN KEY (`UserID`) REFERENCES `USER`(`Id`),
	FOREIGN KEY (`BookID`) REFERENCES `BOOK`(`Id`)
);
